{
  "name": "@daggler/simulate",
  "version": "0.1.0",
  "description": "Pure event-simulation engine: determine which jobs run for a given GitHub Actions event fixture.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@daggler/workflow-ir": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
