{
  "name": "@daggler/inventory",
  "version": "0.1.0",
  "description": "Repo automation-map engine: summarise a repository's entire GitHub Actions surface from raw workflow YAML.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@daggler/workflow-ir": "workspace:*",
    "@daggler/validators": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
