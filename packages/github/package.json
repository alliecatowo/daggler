{
  "name": "@daggler/github",
  "version": "0.1.0",
  "description": "GitHub ports and adapters for Daggler: typed interfaces for GitHub operations and an in-memory fake for tests and local demo mode.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@daggler/workflow-ir": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
