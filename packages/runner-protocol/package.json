{
  "name": "@daggler/runner-protocol",
  "version": "0.1.0",
  "description": "RunnerPort abstraction — the hexagonal execution layer for Daggler. Defines the capability ladder (static analysis → act → GitHub) and ships the real AnalyzerAdapter.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@daggler/workflow-ir": "workspace:*",
    "@daggler/validators": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
