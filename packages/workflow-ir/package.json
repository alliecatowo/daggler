{
  "name": "@daggler/workflow-ir",
  "version": "0.1.0",
  "description": "The heart of Daggler: parse GitHub Actions YAML into a semantic WorkflowIR with source maps, build the graph, and serialize back to native YAML.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
