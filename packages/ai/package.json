{
  "name": "@daggler/ai",
  "version": "0.1.0",
  "description": "Daggler AI assistant core: prompt construction, schema validation, and edit application. Pure — no live Anthropic calls.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@daggler/workflow-ir": "workspace:*",
    "@daggler/validators": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
