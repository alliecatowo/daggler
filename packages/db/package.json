{
  "name": "@daggler/db",
  "version": "0.1.0",
  "description": "Daggler's relational model: Drizzle schema for identity, GitHub installs, workflow revisions/drafts, diagnostics, actions catalog, execution runs, and policies.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate": "drizzle-kit generate"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.1",
    "typescript": "^5.7.2"
  }
}
